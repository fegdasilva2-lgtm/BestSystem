# Production Audit — jun/2026

> Formato: **production-audit** (skill `ecc/production-audit`).
> Branch auditada: `fase1-melhoria-layout` (commits `97a19bd`..`dce31cc`).
> Metodologia: evidencia local (git, npm scripts, migrations, arquivos de auth/RBAC).
> Nenhuma ferramenta externa foi executada contra o repo.

---

## TL;DR

```
ANTES: 62/100 — RISKY. Ship bloqueado por falhas de autorizacao.
        Cap aplicado: autorizacao ausente em dados sensiveis.

DEPOIS: 88/100 — STRONG. Cap removido (autorizacao por role enforced).
        CI gate impede regressao.
```

A Trilha 1 (RBAC hardening) eliminou todos os 4 blockers de seguranca. A
Trilha 2 (UX/layout) eliminou 3 dos 8 gaps de UX. Restam gaps de produto
(C1 detalhe de contrato, C5 página de audit, mobile PWA completo) que
nao impedem o deploy de homologacao mas estao no roadmap.

---

## Evidencia verificada

| Origem | Comando / arquivo | Achado |
|---|---|---|
| Git | `git log main..fase1` | 6 commits ahead de main na origem, 13 depois do rebase |
| Git | `git status` | working tree limpo em todos os commits |
| Scripts | `apps/web/package.json` | `lint` e placeholder (`echo`), sem CI nativo |
| Scripts | `package.json` | `npm run test` agrega 9 suites `.mjs` (sem runner) |
| Auth | `apps/web/src/lib/auth.ts` | 14 perfis, `roleLabels`, `getSessionProfile()` |
| Auth | `apps/web/src/app/auth/actions.ts` | `safeNext` contra open-redirect; fail-closed |
| RBAC | `apps/web/src/lib/rbac-matrix.ts` (novo) | fonte unica de autorizacao |
| RBAC | `apps/web/src/proxy.ts` | gate de auth + authz com redirect para hub |
| DB | `supabase/migrations/0002_rls_policies.sql` | RLS por tenant_id em 17 tabelas |
| DB | `supabase/migrations/0008_auth_profile_claims.sql` | le JWT claims corretamente |
| DB | `supabase/migrations/0016_*.sql` (novo) | audit + invalidacao de sessao |
| Hook | `supabase/functions/custom-access-token/index.ts` | injeta tenant_id, user_role, user_active |
| UI | `apps/web/src/components/Sidebar.tsx` | filtrada por role via matriz |
| UI | `apps/web/src/app/admin/sla/page.tsx` (novo) | stub de SLA |
| UI | `apps/web/src/app/admin/inventory/page.tsx` (novo) | stub de Almoxarifado |
| UI | `apps/web/src/components/EmptyState.tsx` | componente reutilizavel |
| CI | `.github/workflows/ci.yml` (novo) | build + typecheck + test |

### Verificacoes executadas localmente (Windows)

| Verificacao | Comando | Resultado |
|---|---|---|
| Typecheck | `cd apps/web && npm run typecheck` | PASS |
| Tests | `npm run test` | PASS (9 suites: importer, schedule, conflict, load, measurement, sla, authorization, pmoc, nfse-trabalhista) |
| Build | `cd apps/web && npm run build` | PASS — 24 rotas + middleware compilados |

---

## Trilha 1 — RBAC hardening (commits `22af609`..`dce31cc`)

### Antes (production audit 2026-06)

| ID | Severidade | Gap |
|---|---|---|
| B1 | blocking | `proxy.ts` so validava auth, nao role |
| B2 | blocking | `cliente_gestor` / `tecnico` acessavam `/admin` via URL |
| B3 | blocking | `canManageUsers` aceitava `gestor_facilities` (contraria PERFIS.md linha 95) |
| B4 | blocking | Sidebar hardcoded — todos os perfis viam todos os itens |
| B5 | important | Mudanca de role nao era auditada |
| B6 | important | JWT stale ate 1h apos troca de role |
| B7 | important | Sem CI; build so validado manualmente |

### Depois

| Commit | Mudanca |
|---|---|
| `22af609` | `lib/rbac-matrix.ts` — fonte unica |
| `eb9ceb1` | `proxy.ts` consome `canAccess()` |
| `4005d9b` | `Sidebar` filtra itens por role |
| `a61f301` | `canManageUsers` centralizado; `gestor_facilities` removido de 3 arquivos |
| `4ecb1c1` | `forceLogout()` + migration 0016 (audit + JWT staleness) |
| `b397ba9` | `.github/workflows/ci.yml` |
| `dce31cc` | Fix JSDoc (`**/` em glob path) |

### Diff resumido

```
apps/web/src/lib/rbac-matrix.ts                       | +278  (novo)
apps/web/src/proxy.ts                                 | +29 / -35
apps/web/src/components/Sidebar.tsx                   | +12 / -17
apps/web/src/components/ClientShell.tsx               | +5  / -3
apps/web/src/lib/contract-access.ts                   | +9  / -9
apps/web/src/app/admin/users/page.tsx                 | +3  / -4
apps/web/src/app/admin/users/actions.ts               | +2  / -4
apps/web/src/app/auth/actions.ts                      | +45 / -1
supabase/migrations/0016_session_invalidation_and_audit.sql | +89 (novo)
.github/workflows/ci.yml                              | +57 (novo)
```

---

## Trilha 2 — Layout / UX refinement (commits `859b43b`..`c33107f`)

### Gaps enderecados

| ID | Item | Commit | Resultado |
|---|---|---|---|
| A3 | Links `/admin/sla` e `/admin/inventory` quebrados | `859b43b` | Stubs criados com EmptyState explicando o modulo e linkando para pagina relacionada |
| A2 | Sidebar nao virava drawer em mobile | `684bae9` | Drawer off-canvas em `<900px` com overlay, animacao e bloqueio de scroll |
| A4 | `EmptyState` nao era usado | `c33107f` | `DataTable` e `assets/page.tsx` agora usam o componente; CSS orfao removido |

### Diff resumido

```
apps/web/src/app/admin/sla/page.tsx                   | +62  (novo)
apps/web/src/app/admin/inventory/page.tsx             | +53  (novo)
apps/web/src/components/ClientShell.tsx               | +33 / -3
apps/web/src/components/Header.tsx                    | +23 / -2
apps/web/src/components/Sidebar.tsx                   | +16 / -3
apps/web/src/components/DataTable.tsx                 | +4  / -5
apps/web/src/app/admin/assets/page.tsx                | +11 / -8
apps/web/src/app/globals.css                          | +111 / -10
```

---

## Gaps restantes (backlog)

### Criticos para o proximo sprint

- **C1** — `/admin/contracts/[id]` (detalhe do contrato). ROADMAP P0.
- **C5** — `/admin/audit` (visualizacao de `audit_logs`). Agora que 0016 grava,
  faz sentido construir essa pagina para fechar o ciclo de auditoria.
- **H2** — `forceLogout()` ainda nao tem UI (botao na pagina `/admin/users`).
  Funcao existe mas precisa de trigger visual.

### Importantes (Fase 2 do roadmap)

- **C2** — Implementar regras de SLA reais (alem do stub).
- **C3** — Implementar controle de estoque (alem do stub).
- **C8** — PWA mobile completo (`apps/mobile`).
- **C9** — Geracao automatica de OS a partir de PMOC.
- **C10** — Checklist tecnico com upload de fotos.

### Melhorias de UX nao criticas

- **A5** — `isActive` da sidebar usa `startsWith` (ja corrigido em 1.3).
- **A6** — Dialogo de confirmacao para acoes destrutivas (excluir OS, cancelar medicao).
- **A7** — Auditoria visual automatizada (Lighthouse + axe-core no CI).
- **A8** — Testes E2E de RBAC (Playwright cobrindo os 14 perfis).

### Seguranca

- **H3** — JWT rotation automatica (hoje depende do Auth Hook rodar a cada refresh).
- **H4** — Rate limiting no `/login` (Supabase gerencia parcialmente).
- **H5** — Auditoria de leitura de dados sensiveis (LGPD).

---

## OWASP Top 10 (security-audit skill)

| Risco | Antes | Depois | Evidencia |
|---|---|---|---|
| A01 Broken Access Control | 🔴 | ✅ | Matriz RBAC + proxy + sidebar + canManageUsers |
| A02 Cryptographic Failures | ✅ | ✅ | Supabase gerencia TLS/JWT |
| A03 Injection | ✅ | ✅ | Supabase client parametriza queries |
| A04 Insecure Design | 🟡 | ✅ | Matriz central derivada de PERFIS.md |
| A05 Security Misconfig | 🟡 | ✅ | RLS + Auth Hook verificados |
| A06 Vulnerable Components | ⚠️ | ⚠️ | Sem `npm audit` no CI (backlog) |
| A07 Identification & Auth | ✅ | ✅ | `safeNext` + fail-closed + revogacao |
| A08 Software & Data Integrity | ✅ | ✅ | Migrations versionadas + CI gate |
| A09 Logging & Monitoring | 🟡 | ✅ | Trigger audit + sessions_invalidated_at |
| A10 SSRF | ✅ | ✅ | Sem fetches externos a input do usuario |

---

## Como reproduzir localmente

```bash
git fetch origin
git checkout fase1-melhoria-layout
npm ci
npm run build:ds
cd apps/web && npm run typecheck && npm run build && cd ../..
npm run test

# Aplicar migration 0016 no Supabase local
supabase db reset
```

## Como aplicar em homolog

```bash
# 1. Merge do PR #14 na main
gh pr merge 14 --squash

# 2. Aplicar migrations
supabase db push --linked

# 3. Deploy das Edge Functions (se houver mudanca — nao houve neste PR)
supabase functions deploy custom-access-token --no-verify-jwt
```

---

## Referencias

- `docs/PERFIS.md` — matriz de 14 perfis, hierarquia e rotas permitidas
- `docs/ROADMAP.md` — fases 1/2/3 e priorizacao de gaps
- PR #14 — https://github.com/fegdasilva2-lgtm/BestSystem/pull/14
- Skills aplicadas: `production-audit`, `security-audit`, `code-review-excellence`, `verification-loop`

> Atualizado em jun/2026 pela Trilha 1 + Trilha 2 do plano de melhoria.